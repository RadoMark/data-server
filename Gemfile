source "https://rubygems.org"

gem "puma" # server
gem "sinatra" # Web app DSL
gem "json" # core JSON library
gem "require_all" # Helpful require_all(<dir>) method

group :development, :test do
  gem "pry"
  gem "rspec" # BDD framework
end
